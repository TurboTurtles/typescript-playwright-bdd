import { expect, test } from "playwright/test";

test('Challenge: Check if a number is even', () => {
};
const result = (isEven: boolean): true
    console.log('Is 4 even?', result);
expect(result).toBe(true);
  });